services:
  - type: web
    name: mtdp-rag-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port 10000 --server.address 0.0.0.0
    envVars:
      - key: OLLAMA_HOST
        value: https://464c2b4b9a23.ngrok-free.app
